# MCP Mesh URL (primary - required)
MESH_URL=http://localhost:3000

# Mesh API key (if required by your mesh)
# MESH_API_KEY=...

# Default model for AI responses (via mesh's OpenRouter binding)
DEFAULT_MODEL=anthropic/claude-sonnet-4

# WebSocket port for extension connection
WS_PORT=9999

# Allowed paths for terminal commands (comma-separated)
ALLOWED_PATHS=/Users/guilherme/Projects

# Blocked terminal commands (comma-separated patterns)
BLOCKED_COMMANDS=rm -rf,sudo,chmod 777

# Fallback: Direct OpenRouter API key (only used if mesh unavailable)
# OPENROUTER_API_KEY=sk-or-...
